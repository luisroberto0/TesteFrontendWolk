import { AppConfig } from '@uex/web-extensions';

export const CONFIG: AppConfig = {
    template: {
        default: {
            header: true,
        },
        noHeader: {
            header: false,
        },
    },
};
